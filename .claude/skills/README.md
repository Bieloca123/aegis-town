# Project skills

Drop skill folders here (each as `<skill-name>/SKILL.md`) for project-specific workflows that should be invocable as `/skill-name`.

No skills authored yet. Add one when a workflow is repeated often enough that a slash-command shortcut earns its keep.
